#include <stdio.h>

int main (void){

    int x, y, z, a, b, c;

    scanf("%i %i %i",&x,&y,&z);

    if(x > y)
    x = a;

    else if(x > z)
    x = a;

    else if(x > z)

    printf("%i %i %i",a, b, c);

    return 0;
}