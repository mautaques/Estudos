#include <stdio.h>
#include <stdbool.h>
#include <math.h>

int main(void) {

  int N1, N2, primo, i=0;

  scanf("%d",&N1);
  scanf("%d",&N2);
  
  if(N1 >= N2){

  } else{

        for(i=N1;i>N2;i++){   

          if( i % 2 == 0 || i % 3 == 0 || i % 5 == 0){

          } else{

            i = primo
            
            printf("\n%d",primo);
 
          }
    }
            printf("\n%d",i);

  }

  return 0;
}
